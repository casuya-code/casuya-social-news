# Casuya Social News — Server Dependencies
# Python >= 3.12 required (pins verified on Python 3.14.6)

# Web framework
fastapi==0.141.1
uvicorn[standard]==0.52.3
python-multipart==0.0.20

# Database
sqlalchemy[asyncio]==2.0.52
asyncpg==0.31.0              # PostgreSQL async driver
alembic==1.19.1              # Database migrations

# Caching (optional — falls back to in-memory if Redis unavailable)
redis==8.1.0

# Task queue
celery[redis]==5.5.1

# HTTP client (for external APIs)
httpx==0.28.1

# Auth
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4

# Validation
pydantic==2.13.4
pydantic-settings==2.15.0

# Environment
python-dotenv==1.2.2

# NLP / AI
openai==1.85.0              # For GPT-based script generation (alternative to direct API)

# Voice
elevenlabs==2.63.0          # ElevenLabs TTS API
google-cloud-texttospeech==2.20.0  # Google Cloud TTS (default provider)

# Monitoring
prometheus-client==0.21.1

# Logging
structlog==26.1.0           # Structured JSON logging

# Utilities
orjson==3.11.9             # Fast JSON serialization
python-dateutil==2.9.0
jsonschema==4.26.0         # Contract tests: validate scripts vs shared/schemas

# Storage (optional — only needed for the S3 backend; local is default)
boto3==1.36.24

# Development / Testing
pytest==8.3.4
pytest-asyncio==0.25.3
ruff==0.11.8                 # Linter + formatter